//REQUISITE IMPORTS HERE

//VARIABLE NAMES FOR ACTION TYPES HERE

//FRONT-END AUTHENTICATION FUNCTIONS HERE

//ACTION-CREATOR FUNCTIONS HERE

export default function reducer(state = {}, action) {
  Object.freeze(state);
  // const newState = Object.assign({}, state);
  switch (action.type) {
    // INSERT CASES HERE 
    default: return state;
  }
}